package ex3_set;
//Set

/*
 * List와 다르게 객체의 순서를 저장하지 않는다
 * 수학의 집합과 유사한 개념을 가지고 있다.
 * 데이터의 중복을 허용하지 않는다.
 * 데이터를 저장할 때 순서, 즉 index가 없어 데이터가 입력된 순서대로 출력된단 보장이 없다.
 * 
 * 		메서드						기능
 * void add(E e)				데이터를 순차적으로 삽입
 * void remove(Objcect o)		선택된 값 삭제
 * void clear()					모든 데이터삭제
 * int size()					저장된 데이터 개수 반환
 * boolean contains(Object o)	데이터 존재 여부 확인
 * 
 * -HashSet
 * 
 * Set인터페이스에서 가장 많이 사용되는 클래스로 인터페이스를 구현한 클래스이다.
 * 
 */
public class set {

}
