package ex1_class;

public class Car {
	//클래스의 구성
	//1. 필드
	//객체가 가져야할 데이터의 상태를 저장하는 변수
	//필드, 전역변수, 멤버 변수라고 부른다.
	//필드의 값을 초기화하지 않으면 객체 생성 시 기본값으로 초기화 된다.
	
	//2. 메서드
	
	int whell = 4; // 초기화를 할 수 있다.
	
	//필드는 클래스에 포함된 요소이자, 객체를 생성한 후 객체가 가지는
	//데이터이기도 하다.
	//따라서 객체를 생성한 후 그 객체의 필드를 사용할 수 있다.

}
